import BuyerGroupOrdersClient from '@/components/portal/BuyerGroupOrdersClient';import {getLocale} from '@/lib/i18n-server';
export const dynamic='force-dynamic';
export default async function Page(){const locale=await getLocale();const en=locale==='en';return <main className="portal-page"><p className="eyebrow">{en?'GROUP BUYING':'KÖZÖS BESZERZÉS'}</p><h1>{en?'Large lot, smaller commitment':'Nagy tétel, kisebb részesedés'}</h1><p className="portal-lead">{en?'Reserve pallets, cartons, kilograms or units. The fill percentage shows how much remains before the wholesale MOQ is reached.':'Foglalj raklapot, kartont, kilogrammot vagy más mennyiséget. A kampány százaléka mutatja, mennyi hiányzik még a teljes nagykereskedelmi MOQ eléréséhez.'}</p><BuyerGroupOrdersClient locale={locale}/></main>}
