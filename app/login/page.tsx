import Link from 'next/link';
import { Building2, LockKeyhole, UserRound } from 'lucide-react';
import { getLocale } from '@/lib/i18n-server';
import LanguageSwitcher from '@/components/LanguageSwitcher';

export default async function Page(){const locale=await getLocale();const en=locale==='en';return <main className="auth-page"><section className="role-choice"><div className="auth-card-tools"><div><p className="eyebrow">{en?'LOG IN':'BELÉPÉS'}</p><h1>{en?'Choose your BulkPact portal':'Válaszd ki a BulkPact felületed'}</h1></div><LanguageSwitcher locale={locale}/></div><div className="portal-link-grid"><Link href="/buyer/login"><UserRound/><div><strong>{en?'Buyer login':'Vevői belépés'}</strong><span>{en?'Join Group Orders, manage commitments and track fulfilment.':'Csatlakozz Group Orderekhez, kezeld a foglalásokat és kövesd a teljesítést.'}</span></div></Link><Link href="/supplier/login"><Building2/><div><strong>{en?'Supplier login':'Beszállítói belépés'}</strong><span>{en?'Submit high-MOQ offers and track aggregated demand.':'Adj be magas MOQ-s ajánlatokat és kövesd az aggregált keresletet.'}</span></div></Link><Link href="/admin/login"><LockKeyhole/><div><strong>{en?'Admin login':'Admin belépés'}</strong><span>{en?'Group Orders, users, suppliers, payments, operations and analytics.':'Group Orderek, felhasználók, beszállítók, fizetések, operáció és analitika.'}</span></div></Link></div><Link className="auth-back" href="/">← {en?'Home':'Főoldal'}</Link></section></main>}
