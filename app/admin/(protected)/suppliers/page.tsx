import RegisteredManufacturersClient from '@/components/portal/RegisteredManufacturersClient';import{getLocale}from'@/lib/i18n-server';export default async function Page(){const locale=await getLocale();const en=locale==='en';return <main className="portal-page"><p className="eyebrow">{en?'PARTNER VERIFICATION':'PARTNER-ELLENŐRZÉS'}</p><h1>{en?'Registered suppliers':'Regisztrált beszállítók'}</h1><p className="portal-lead">{en?'Suppliers can create their own profiles, but they become active only after admin approval.':'A beszállítók önállóan regisztrálhatnak, de csak admin jóváhagyás után kapnak aktív státuszt.'}</p><RegisteredManufacturersClient locale={locale}/></main>}
