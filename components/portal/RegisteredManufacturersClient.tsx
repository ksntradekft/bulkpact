'use client';
import { useCallback,useEffect,useState } from 'react';import { ExternalLink,LoaderCircle,RefreshCw } from 'lucide-react';import type{Locale}from'@/lib/i18n-shared';
export default function RegisteredManufacturersClient({locale='hu'}:{locale?:Locale}){const en=locale==='en';const[items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true);const load=useCallback(()=>{setLoading(true);fetch('/api/admin/suppliers').then(r=>r.json()).then(d=>setItems(d.manufacturers||[])).finally(()=>setLoading(false))},[]);useEffect(load,[load]);async function patch(id:string,approval_status:string){await fetch('/api/admin/suppliers',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,approval_status})});load()}return <section className="manufacturer-panel"><div className="manufacturer-toolbar"><strong>{items.length} {en?'registered suppliers':'regisztrált gyártó'}</strong><button className="icon-button" onClick={load} aria-label={en?'Refresh':'Frissítés'}><RefreshCw size={17}/></button></div>{loading?<div className="empty-state"><LoaderCircle className="spin"/></div>:<div className="manufacturer-table-wrap"><table className="manufacturer-table"><thead><tr><th>{en?'Company':'Cég'}</th><th>{en?'Country':'Ország'}</th><th>{en?'Categories':'Kategóriák'}</th><th>MOQ</th><th>{en?'Certifications':'Tanúsítványok'}</th><th>{en?'Approval':'Jóváhagyás'}</th><th>Web</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.profiles?.company_name||'–'}</strong><small>{x.profiles?.email}</small></td><td>{x.profiles?.country||'–'}</td><td>{(x.categories||[]).join(', ')||'–'}</td><td>{x.moq_text||'–'}</td><td>{(x.certifications||[]).join(', ')||'–'}</td><td><select value={x.approval_status} onChange={e=>patch(x.id,e.target.value)}><option value="PENDING">{en?'Under review':'Ellenőrzés alatt'}</option><option value="APPROVED">{en?'Approved':'Jóváhagyva'}</option><option value="REJECTED">{en?'Rejected':'Elutasítva'}</option></select></td><td>{x.website?<a className="icon-button" href={x.website} target="_blank" rel="noreferrer"><ExternalLink size={17}/></a>:'–'}</td></tr>)}</tbody></table></div>}</section>}
